{
    "name": "bitrix/mcp-server",
    "description": "MCP server for Bitrix infoblocks and highload blocks (read/write)",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "mcp/sdk": "^0.6"
    },
    "autoload": {
        "psr-4": {
            "BitrixMcp\\": "src/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
